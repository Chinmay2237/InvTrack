name: invtrack
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  go_router: ^17.0.0
  provider: ^6.1.1
  google_fonts: ^6.1.0
  firebase_core: ^4.2.1
  cloud_firestore: ^6.1.0
  firebase_storage: ^13.0.4
  cupertino_icons: ^1.0.2
  firebase_auth: ^6.1.2
  shared_preferences: ^2.2.2
  image_picker_for_web: ^3.0.0
  intl: ^0.20.2
  url_launcher: ^6.2.1
  file_picker: ^6.1.1
  csv: ^5.0.0
  firebase_ai: ^3.6.0
  shake_animation: ^1.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true